# core pipeline
yt-dlp>=2026.1
openai>=2.0            # OpenAI-compatible client for the GLM endpoint
requests
python-dotenv
psycopg[binary]>=3.2
pgvector>=0.5
sentence-transformers>=3.0   # all-MiniLM-L6-v2 text embedder (384-dim)
ImageHash>=4.3
Pillow

# API layer
fastapi
uvicorn
python-multipart      # multipart parsing for the POST /uploads endpoint

# frontend
streamlit>=1.50

# tests
httpx
