{
  "name": "lusora",
  "private": true,
  "version": "0.1.0",
  "description": "Automated, multi-channel YouTube video production platform",
  "scripts": {
    "validate:schemas": "node scripts/validate-schemas.mjs",
    "lint:boundaries": "node scripts/lint-boundaries.mjs",
    "doctor": "node scripts/doctor.mjs",
    "typecheck": "pnpm -r --filter './contracts' --filter './engine' --filter './platform' run typecheck",
    "test": "pnpm -r run test",
    "catalog:check": "pnpm --filter @lusora/engine run catalog:check",
    "ci": "pnpm run validate:schemas && pnpm run lint:boundaries && pnpm run typecheck && pnpm run test && pnpm run catalog:check",
    "edit-pass-prompt": "pnpm --silent --filter @lusora/platform run edit-pass-prompt",
    "ab:fork": "pnpm --silent --filter @lusora/platform run ab:fork"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.9.0"
  },
  "packageManager": "pnpm@11.14.0"
}
